spring:
    application.name: ms-discovery
    profiles.active: defaults
    cloud.config:
        uri: http://localhost:8888/cloud
        server.prefix: cloud
        # username: root
        # password: s3cr3t

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false